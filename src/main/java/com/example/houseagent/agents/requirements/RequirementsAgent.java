package com.example.houseagent.agents.requirements;

import com.example.houseagent.agents.api.Agent;
import com.example.houseagent.domain.model.HouseRequirements;
import com.example.houseagent.domain.model.UserRequest;

public interface RequirementsAgent extends Agent<UserRequest, HouseRequirements> {
}
